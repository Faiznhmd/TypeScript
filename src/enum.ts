enum Direction {
  UP = 8,
  DOWN,
  LEFT,
  RIGHT,
}

let dir: Direction = Direction.DOWN;
// automatically get a value hover in down u will see the value if we write a value 8 in up then the value of down is 9

//enum string

enum Status {
  Pending = 'PENDING',
  Approved = 'APPROVED',
  Rejected = 'REJECTED',
}

let stat: Status = Status.Approved;

// only value we will get add on the enum
